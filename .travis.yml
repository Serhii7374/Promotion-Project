language: node_js
os:
  - osx
osx_image: xcode14.2
node_js:
  - 18.18.0
addons:
  chrome: stable
cache:
  directories:
    - node_modules
    - /Users/travis/Library/Caches/Cypress
install:
  - npm install --legacy-peer-deps
before_script:
  - npm install cypress --force
script:
  - npm run build
  - npm run test
  - npm install cypress --force
  - npm run cypress:run

  - firebase use --add promotion-project-e8793
  - firebase deploy --token "$FIREBASE_TOKEN"
